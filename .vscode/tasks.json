{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BuildArray",
            "type": "shell",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/Array"  // 设置工作目录为 Array
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
